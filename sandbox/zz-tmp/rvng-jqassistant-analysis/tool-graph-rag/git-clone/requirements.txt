# --- Core API & MCP (Forced Pydantic update for Python 3.14+) ---
fastapi
fastmcp==2.10.6
pydantic>=2.9.0,<2.12.0        # <--- This strictly prevents the 2.12 breaking change
pydantic-settings>=2.5.0       # Forces a modern version compatible with newer Python

# --- Graph & LLM ---
neo4j==5.28.1
litellm
google-adk==1.21.0
tiktoken>=0.9.0

# --- AST & Parsing ---
tree_sitter==0.25.2
tree_sitter_java==0.23.5
tree_sitter_kotlin==1.1.0

# --- Embeddings ---
sentence_transformers==4.1.0
transformers>=4.40.0
tqdm==4.67.1

# --- System ---
certifi>=2023.0.0

# Optional: torch is required by sentence-transformers for local models. Installing
# torch is platform specific; uncomment and install the appropriate wheel for your
# environment (CPU/GPU). Example CPU wheel install:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.2.0
# torch==2.2.0
