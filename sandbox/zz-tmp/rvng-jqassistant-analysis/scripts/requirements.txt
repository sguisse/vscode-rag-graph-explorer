# Root Python requirements for repository tools (installer, verifier, MCP server, RAG helpers)
# Install with: python -m pip install -r requirements.txt

# YAML parsing used by verifier
pyyaml>=6.0

# HTTP client used by various scripts (LLM client, download helpers)
requests>=2.31.0

# Neo4j Python driver used by graph utilities
neo4j>=5.8.0

# MCP server package (JSON-RPC / fastmcp implementation)
mcp>=1.27.0

# FastAPI-based helpers / shim
fastapi>=0.95.0
uvicorn[standard]>=0.23.0

# Local embeddings (sentence-transformers). Note: this pulls in heavy deps (torch).
sentence-transformers>=2.2.2
transformers>=4.40.0

# Optional: torch is required by sentence-transformers for local models. Installing
# torch is platform specific; uncomment and install the appropriate wheel for your
# environment (CPU/GPU). Example CPU wheel install:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.2.0
# torch==2.2.0

# dotenv for loading skill.env files
python-dotenv>=1.0.0
